@model IEnumerable<ProyectoºMVC.Models.roles>

<select class="form-control" id="idRol" name="idRol">
    @foreach (var kev in Model)
    {
<option value="@kev.id">@kev.descripcion</option>}
</select>
